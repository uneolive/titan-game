# API Configuration
VITE_API_BASE_URL=https://devapi-submittalassistant.pimtrackapp.co

# MOCK MODE: Currently using mock client for testing
# All services are configured to use mockClient.ts instead of real API
# To switch to real backend:
# 1. Ensure backend is running on the URL above
# 2. In each service file, comment the MOCK import and uncomment the REAL import

